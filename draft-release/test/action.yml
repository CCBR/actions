name: draft-release integration test
description: Verify the draft-release action runs in dry-run mode without creating release artifacts

runs:
  using: composite
  steps:
    - uses: ./draft-release
      with:
        github-token: ${{ github.token }}
        version-tag: v0.8.1
        ccbr-actions-version: main
        dry-run: true
