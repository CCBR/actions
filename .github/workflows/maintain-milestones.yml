#
# Workflow: Maintain Milestones
#
# Purpose
# - Keep monthly milestones aligned to a rolling cadence for a target repository.
# - Ensure milestones exist for all months in the current UTC year plus January of next year.
# - Archive stale monthly milestones from prior years by closing them if still open.
#
# Milestone Naming and Scope
# - Managed milestones follow strict title format: YYYY-MM (example: 2026-07).
# - Only titles matching YYYY-MM are managed by this workflow.
# - Non-monthly milestone titles are ignored.
#
# Triggers
# - workflow_dispatch: manual trigger from Actions UI or API.
# - workflow_call: reusable workflow trigger from other workflows.
#
# Inputs
# - target_repo (string, optional): owner/repo to manage.
#   - Default: current repository running the workflow.
# - dry_run (boolean, optional):
#   - true: compute/report planned actions only (no writes).
#   - false: apply actions to milestones via GitHub API.
#   - Default: true.
# - max_updates_per_run (number, optional):
#   - Safety limit for number of planned actions in a single run.
#   - Workflow fails if planned actions exceed this value.
#   - Default: 100.
#
# Secrets / Auth
# - repo_token (optional): token with milestone write access for target_repo.
# - Fallback: github.token if repo_token is not supplied.
#
# Required Permissions
# - contents: read
# - issues: write (required for create/update/close/reopen milestones)
#
# Due Date Behavior
# - Expected due date for each monthly milestone is the last calendar day of that month.
# - `due_on` is set to 12:00:00 UTC on the due date to avoid timezone rendering shifts
#   that can show the date as one day early in western time zones.
#
# Planned Action Types
# - close_old: close open YYYY-MM milestones where year < current UTC year.
# - create: create missing expected milestones with computed due date.
# - reopen: reopen expected milestones found in closed state.
# - set_due_date: update due date if existing due date differs from expected.
#
# Output / Reporting
# - Writes a summary table with target repo, current UTC year, dry-run mode,
#   max updates, planned action count, and per-milestone action rows.
# - Logs same summary via core.info for run logs.
#
# Notes
# - Idempotent by design: repeated runs should converge to zero planned changes.
# - Uses UTC year/month calculations to avoid host-timezone variability.
#
name: Maintain Milestones

on:
  workflow_call:
    inputs:
      target_repo:
        description: Target repository in owner/repo format
        required: false
        default: ""
        type: string
      dry_run:
        description: If true, report changes without applying them
        required: false
        default: true
        type: boolean
      max_updates_per_run:
        description: Maximum milestone updates/creates/closes/reopens per run
        required: false
        default: 100
        type: number
    secrets:
      repo_token:
        description: Token with milestone write access to the target repository
        required: false

  workflow_dispatch:
    inputs:
      target_repo:
        description: Target repository in owner/repo format (defaults to current repo)
        required: false
        default: ""
        type: string
      dry_run:
        description: If true, report changes without applying them
        required: false
        default: true
        type: boolean
      max_updates_per_run:
        description: Maximum milestone updates/creates/closes/reopens per run
        required: false
        default: 100
        type: number

permissions:
  contents: read
  issues: write

jobs:
  maintain-milestones:
    runs-on: ubuntu-latest
    steps:
      - name: Archive old milestones and ensure current cadence
        uses: actions/github-script@v7
        env:
          TARGET_REPO: ${{ inputs.target_repo }}
          DRY_RUN: ${{ inputs.dry_run }}
          MAX_UPDATES: ${{ inputs.max_updates_per_run }}
        with:
          github-token: ${{ secrets.repo_token || github.token }}
          script: |
            const target = (process.env.TARGET_REPO || context.repo.owner + '/' + context.repo.repo).trim();
            const dryRun = String(process.env.DRY_RUN).toLowerCase() === 'true';
            const maxUpdatesRaw = Number(process.env.MAX_UPDATES || '100');
            const maxUpdates = Number.isFinite(maxUpdatesRaw) && maxUpdatesRaw > 0
              ? Math.floor(maxUpdatesRaw)
              : 100;

            const [owner, repo] = target.split('/');
            if (!owner || !repo) {
              core.setFailed(`Invalid target_repo: "${target}". Use owner/repo.`);
              return;
            }

            const now = new Date();
            const currentYear = now.getUTCFullYear();
            const nextYear = currentYear + 1;

            const toTitle = (year, month) => `${year}-${String(month).padStart(2, '0')}`;
            const isMonthlyTitle = (title) => /^\d{4}-\d{2}$/.test(title || '');
            const getYearMonth = (title) => {
              const [y, m] = title.split('-').map((v) => Number(v));
              return { year: y, month: m };
            };
            const isValidMonth = (month) => month >= 1 && month <= 12;
            const lastDayOfMonth = (year, month) => {
              // month is 1..12; Date.UTC month index is 0..11; day 0 gives previous month's last day.
              return new Date(Date.UTC(year, month, 0)).getUTCDate();
            };
            const dueDateOnly = (year, month) => {
              const day = String(lastDayOfMonth(year, month)).padStart(2, '0');
              return `${year}-${String(month).padStart(2, '0')}-${day}`;
            };
            // Use midday UTC to avoid date shifting to the previous day in western timezones.
            const dueOnPayload = (dateOnly) => `${dateOnly}T12:00:00Z`;
            const dueOnDateOnly = (dueOn) => (dueOn ? String(dueOn).slice(0, 10) : null);

            const desiredTitles = new Set();
            for (let month = 1; month <= 12; month += 1) {
              desiredTitles.add(toTitle(currentYear, month));
            }
            desiredTitles.add(toTitle(nextYear, 1));

            const milestones = await github.paginate(github.rest.issues.listMilestones, {
              owner,
              repo,
              state: 'all',
              per_page: 100,
            });

            const byTitle = new Map();
            for (const ms of milestones) {
              byTitle.set(ms.title, ms);
            }

            const plan = [];
            const addPlan = (kind, title, details = {}) => {
              plan.push({ kind, title, ...details });
            };

            for (const ms of milestones) {
              if (!isMonthlyTitle(ms.title)) {
                continue;
              }
              const { year, month } = getYearMonth(ms.title);
              if (!Number.isInteger(year) || !Number.isInteger(month) || !isValidMonth(month)) {
                continue;
              }

              if (year < currentYear && ms.state === 'open') {
                addPlan('close_old', ms.title, { milestone_number: ms.number });
              }
            }

            for (const title of desiredTitles) {
              const { year, month } = getYearMonth(title);
              const expectedDueDate = dueDateOnly(year, month);
              const expectedDueOn = dueOnPayload(expectedDueDate);
              const existing = byTitle.get(title);

              if (!existing) {
                addPlan('create', title, { due_on: expectedDueOn });
                continue;
              }

              if (existing.state === 'closed') {
                addPlan('reopen', title, { milestone_number: existing.number });
              }

              const existingDateOnly = dueOnDateOnly(existing.due_on);
              if (existingDateOnly !== expectedDueDate) {
                addPlan('set_due_date', title, {
                  milestone_number: existing.number,
                  due_on: expectedDueOn,
                  from_due_on: existing.due_on,
                });
              }
            }

            if (plan.length > maxUpdates) {
              core.setFailed(
                `Planned ${plan.length} updates, exceeding max_updates_per_run=${maxUpdates}. ` +
                'Increase input or run again after partial cleanup.'
              );
              return;
            }

            const results = [];
            const runOrLog = async (action, fn) => {
              if (dryRun) {
                results.push({ ...action, status: 'planned' });
                return;
              }
              await fn();
              results.push({ ...action, status: 'applied' });
            };

            for (const action of plan) {
              if (action.kind === 'close_old') {
                await runOrLog(action, async () => {
                  await github.rest.issues.updateMilestone({
                    owner,
                    repo,
                    milestone_number: action.milestone_number,
                    state: 'closed',
                  });
                });
                continue;
              }

              if (action.kind === 'create') {
                await runOrLog(action, async () => {
                  await github.rest.issues.createMilestone({
                    owner,
                    repo,
                    title: action.title,
                    due_on: action.due_on,
                    state: 'open',
                  });
                });
                continue;
              }

              if (action.kind === 'reopen') {
                await runOrLog(action, async () => {
                  await github.rest.issues.updateMilestone({
                    owner,
                    repo,
                    milestone_number: action.milestone_number,
                    state: 'open',
                  });
                });
                continue;
              }

              if (action.kind === 'set_due_date') {
                await runOrLog(action, async () => {
                  await github.rest.issues.updateMilestone({
                    owner,
                    repo,
                    milestone_number: action.milestone_number,
                    due_on: action.due_on,
                  });
                });
              }
            }

            const lines = [];
            lines.push(`Target repository: ${owner}/${repo}`);
            lines.push(`Current UTC year: ${currentYear}`);
            lines.push(`Dry run: ${dryRun}`);
            lines.push(`Max updates per run: ${maxUpdates}`);
            lines.push(`Planned changes: ${plan.length}`);
            lines.push('');

            if (results.length === 0) {
              lines.push('No changes needed.');
            } else {
              lines.push('| Action | Milestone | Status |');
              lines.push('|---|---|---|');
              for (const r of results) {
                lines.push(`| ${r.kind} | ${r.title} | ${r.status} |`);
              }
            }

            core.summary.addCodeBlock(lines.join('\n')).write();
            core.info(lines.join('\n'));
