---
title: install-r-pak
format: gfm
execute:
  echo: false
  output: asis
---


```{python}
import ccbr_actions.docs

action = ccbr_actions.docs.parse_action_yaml('action.yml')
print(ccbr_actions.docs.action_markdown_desc(action))
```

Install R package dependencies with pak

## Usage

This action must run on a host runner, not in a job `container`: it provisions R
and pandoc with [`r-lib/actions`](https://github.com/r-lib/actions), which
requires `sudo`.

The repository must have a `DESCRIPTION` file at the root with any dependencies
listed.

Additional dependencies can optionally be recorded in a `versions-file`, which
will be read and passed along to the `extra-packages` argument of
[`setup-r-dependencies`](https://github.com/r-lib/actions/tree/v2-branch/setup-r-dependencies).

### Basic example

[R-CMD-check.yml](/examples/R-CMD-check.yml)

```yaml
steps:
  - uses: actions/checkout@v4
  - uses: CCBR/actions/install-r-pak@main
    with:
      extra-packages: local::.
```

### Customized inputs

[R-CMD-check.yml](/examples/R-CMD-check.yml)

```yaml
jobs:
  R-CMD-check:
    strategy:
      fail-fast: false
      matrix:
        config:
          - { os: ubuntu-latest, r: 'release' }
          - { os: ubuntu-latest, r: 'oldrel-1' }

    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.os }} (${{ matrix.config.r }})
    steps:
      - uses: actions/checkout@v4

      - uses: CCBR/actions/install-r-pak@main
        with:
          versions-file: .github/package-versions.txt
          extra-packages: local::.
          needs: dev
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
```

```{python}
print(ccbr_actions.docs.action_markdown_io(action))
```
